% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/twsa_vbp_plots.R
\name{twsa_vbp_plot}
\alias{twsa_vbp_plot}
\title{TWSA VBP Heatmap Plot}
\usage{
twsa_vbp_plot(
  results,
  twsa_name = NULL,
  groups = "overall",
  comparators = "all",
  wtp = NULL,
  title = NULL,
  xlab = NULL,
  ylab = NULL,
  viridis_option = "viridis",
  show_base_case = TRUE
)
}
\arguments{
\item{results}{TWSA results object from run_twsa() with VBP enabled}

\item{twsa_name}{Name of specific TWSA analysis to plot (NULL for first/only)}

\item{groups}{Group selection: "overall" (default), "all", "all_groups", or
specific group name(s)}

\item{comparators}{Comparator selection: "all" (default, shows each comparator + aggregate),
"overall" (aggregate only), "all_comparators" (individuals only), or specific names}

\item{wtp}{Willingness-to-pay threshold. If NULL, extracts from VBP metadata.}

\item{title}{Optional plot title}

\item{xlab}{Optional x-axis label (defaults to X parameter name)}

\item{ylab}{Optional y-axis label (defaults to Y parameter name)}

\item{viridis_option}{Viridis color palette: "viridis" (default), "magma",
"plasma", "inferno", or "cividis"}

\item{show_base_case}{Logical. Show base case point marker? (default: TRUE)}
}
\value{
A ggplot2 object
}
\description{
Creates a heatmap visualization showing the Value-Based Price (VBP)
across a 2D grid of parameter values. VBP = slope x WTP + intercept.
}
\examples{
\dontrun{
results <- run_twsa(model, vbp_price_variable = "price",
                    vbp_intervention = "treatment")

# VBP heatmap at specific WTP
twsa_vbp_plot(results, wtp = 50000)

# VBP heatmap using WTP from metadata
twsa_vbp_plot(results, comparators = "standard_care")
}
}
