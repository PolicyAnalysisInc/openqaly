% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transition_tables.R
\name{transition_matrix_table}
\alias{transition_matrix_table}
\title{Format Transition Matrix as Table}
\usage{
transition_matrix_table(
  results,
  cycle = 1,
  strategies = NULL,
  groups = "overall",
  decimals = 4,
  font_size = 11,
  table_format = c("flextable", "kable")
)
}
\arguments{
\item{results}{A openqaly model results object}

\item{cycle}{Integer. Which cycle's transition matrix to display (default: 1).}

\item{strategies}{Character vector of strategies to include (NULL for all)}

\item{groups}{Group selection: "overall" (default), specific group name, vector of groups, or NULL}

\item{decimals}{Number of decimal places for probabilities (default: 4)}

\item{font_size}{Font size for the table (default: 11)}

\item{table_format}{Character. Backend to use: "flextable" (default) or "kable"}
}
\value{
A table object (flextable or kable depending on table_format)
}
\description{
Creates a publication-quality table showing the transition probability matrix
for a specific cycle. Rows represent from-states and columns represent
to-states. When multiple strategies are present, they are separated by
spacer columns with hierarchical headers.
}
\examples{
\dontrun{
model <- read_model(system.file("models/example_psm", package = "openqaly"))
results <- run_model(model)

# Create transition matrix table for cycle 1
transition_matrix_table(results)

# Show cycle 5 with kable backend
transition_matrix_table(results, cycle = 5, table_format = "kable")
}

}
