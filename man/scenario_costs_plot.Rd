% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenario_plots.R
\name{scenario_costs_plot}
\alias{scenario_costs_plot}
\title{Plot Scenario Costs as Bar Chart}
\usage{
scenario_costs_plot(
  results,
  summary_name,
  groups = "overall",
  strategies = NULL,
  interventions = NULL,
  comparators = NULL,
  discounted = TRUE,
  axis_decimals = NULL,
  label_decimals = NULL,
  abbreviate = FALSE
)
}
\arguments{
\item{results}{A openqaly scenario results object (output from run_scenario)}

\item{summary_name}{Name of the summary to plot (e.g., "total_cost")}

\item{groups}{Group selection: "overall" (default), specific group name, vector of groups,
or NULL (all groups plus aggregated)}

\item{strategies}{Character vector of strategy names to include (NULL for all).
Mutually exclusive with interventions/comparators.}

\item{interventions}{Character vector of intervention strategy name(s).
Can be combined with comparators for N×M comparisons. When specified, shows
differences (intervention - comparator).}

\item{comparators}{Character vector of comparator strategy name(s).
Can be combined with interventions for N×M comparisons.}

\item{discounted}{Logical. Use discounted values? (default: FALSE)}

\item{axis_decimals}{Fixed decimal places for axis labels, or NULL for auto-precision}

\item{label_decimals}{Fixed decimal places for value labels, or NULL for auto-precision}

\item{abbreviate}{Logical. Use abbreviated number format (K/M/B/T)? (default: FALSE)}
}
\value{
A ggplot2 object
}
\description{
Creates a horizontal bar chart showing the cost value for each scenario.
Scenarios are displayed on the Y-axis with values on the X-axis.
The Base Case is always shown first and highlighted.
}
\examples{
\dontrun{
model <- define_model("markov") \%>\%
  add_scenario("Optimistic") \%>\%
  add_scenario_variable("Optimistic", "efficacy", 0.95)
results <- run_scenario(model)

# Basic cost bar chart
scenario_costs_plot(results, "total_cost")

# Show differences vs comparator
scenario_costs_plot(results, "total_cost", comparators = "control")
}

}
