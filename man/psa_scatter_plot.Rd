% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/psa_plots.R
\name{psa_scatter_plot}
\alias{psa_scatter_plot}
\title{Plot PSA Scatter Plot}
\usage{
psa_scatter_plot(
  results,
  health_outcome,
  cost_outcome,
  groups = "overall",
  strategies = NULL,
  show_means = TRUE,
  alpha = 0.3,
  title = NULL,
  xlab = NULL,
  ylab = NULL,
  legend_position = "right",
  cost_axis_decimals = NULL,
  outcome_axis_decimals = NULL,
  abbreviate = FALSE
)
}
\arguments{
\item{results}{A openqaly PSA results object}

\item{health_outcome}{Name of the health outcome to plot}

\item{cost_outcome}{Name of the cost outcome to plot}

\item{groups}{Group selection: "overall" (default) uses overall/aggregated
results, "all" includes overall + all groups, "all_groups" includes all
groups without overall, and specific group name(s) filter to those groups.}

\item{strategies}{Character vector of strategy names to include (NULL for all)}

\item{show_means}{Logical. Show mean values as larger points? Default is TRUE}

\item{alpha}{Transparency of scatter points (0-1). Default is 0.3}

\item{title}{Plot title. If NULL (default), no title is shown}

\item{xlab}{X-axis label. If NULL (default), uses the health outcome display
name when available}

\item{ylab}{Y-axis label. If NULL (default), uses the cost outcome display
name when available}

\item{legend_position}{Legend position: "right" (default), "bottom", "top",
"left", or "none"}

\item{cost_axis_decimals}{Fixed decimal places for cost axis labels, or NULL for auto-precision}

\item{outcome_axis_decimals}{Fixed decimal places for outcome axis labels, or NULL for auto-precision}

\item{abbreviate}{Logical. Use abbreviated number format (K/M/B/T)? (default: FALSE)}
}
\value{
A ggplot2 object
}
\description{
Creates a scatter plot showing the distribution of costs and outcomes
from PSA simulations. Each point represents one simulation, colored by strategy.
}
\details{
The scatter plot shows each PSA simulation as a point, colored by strategy.
Mean values for each strategy are shown as larger, highlighted points to
indicate the central tendency of the distribution.

PSA scatter plots always use discounted values as this is a cost-effectiveness measure.
}
\examples{
\dontrun{
model <- read_model(system.file("models/example_psm", package = "openqaly"))
psa_results <- run_psa(model, n_sim = 1000)

# Basic scatter plot
psa_scatter_plot(psa_results, "total_qalys", "total_cost")

# Without mean points
psa_scatter_plot(psa_results, "total_qalys", "total_cost", show_means = FALSE)
}

}
