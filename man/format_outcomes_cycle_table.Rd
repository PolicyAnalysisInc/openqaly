% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outcome_tables.R
\name{format_outcomes_cycle_table}
\alias{format_outcomes_cycle_table}
\title{Format Outcomes as Cycle-by-Cycle Table}
\usage{
format_outcomes_cycle_table(
  results,
  group = "aggregated",
  strategies = NULL,
  values = NULL,
  cycles = NULL,
  cumulative = TRUE,
  decimals = 2,
  discounted = FALSE,
  value_name_field = "display_name",
  strategy_name_field = "display_name",
  group_name_field = "display_name",
  time_unit = "cycle"
)
}
\arguments{
\item{results}{A heRomod2 model results object}

\item{group}{Group selection: "aggregated", specific group, or NULL (all groups + aggregated)}

\item{strategies}{Character vector of strategies to include (NULL for all)}

\item{values}{Character vector of value names to include (NULL for all outcomes)}

\item{cycles}{Integer vector or range of cycles to display (NULL for all)}

\item{cumulative}{Logical. Show cumulative totals (TRUE, default) or per-cycle amounts (FALSE)}

\item{decimals}{Number of decimal places (default: 2)}

\item{discounted}{Logical. Use discounted values?}

\item{value_name_field}{Which value name field to use}

\item{strategy_name_field}{Which strategy name field to use}

\item{group_name_field}{Which group name field to use}

\item{time_unit}{Time unit: "cycle", "day", "week", "month", "year"}
}
\value{
A flextable object
}
\description{
Creates a flextable showing outcome values for each cycle.
Supports three display modes with hierarchical headers.
}
\examples{
\dontrun{
model <- read_model(system.file("models/example_psm", package = "heRomod2"))
results <- run_model(model)

# Aggregated cumulative outcomes
ft <- format_outcomes_cycle_table(results)

# All groups with faceting
ft <- format_outcomes_cycle_table(results, group = NULL)

# Save to Word
save_as_docx(ft, path = "outcomes.docx")
}

}
\keyword{internal}
