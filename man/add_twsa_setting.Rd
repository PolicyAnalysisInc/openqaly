% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_builder.R
\name{add_twsa_setting}
\alias{add_twsa_setting}
\title{Add Setting to Two-Way Sensitivity Analysis}
\usage{
add_twsa_setting(
  model,
  twsa_name,
  setting,
  type,
  min = NULL,
  max = NULL,
  radius = NULL,
  steps = NULL,
  values = NULL,
  display_name = NULL
)
}
\arguments{
\item{model}{An oq_model_builder object}

\item{twsa_name}{Name of the TWSA analysis to add the setting to (must exist)}

\item{setting}{Name of the setting to vary (e.g., "discount_cost", "timeframe")}

\item{type}{Type of range specification: "range", "radius", or "custom"}

\item{min}{For type="range": minimum value}

\item{max}{For type="range": maximum value}

\item{radius}{For type="radius": distance from base value}

\item{steps}{For type="range" or "radius": number of steps}

\item{values}{For type="custom": numeric vector of values to use}

\item{display_name}{Optional display name for plots and tables}
}
\value{
The modified model object
}
\description{
Add a model setting to vary in a two-way sensitivity analysis. Each TWSA must
have exactly two parameters (variables or settings). Settings include parameters
like discount rates, timeframe, and other global model configuration values.
}
\details{
Values can be specified using three different range types:

- `type = "range"`: Vary from `min` to `max` in `steps` evenly-spaced values
- `type = "radius"`: Vary from `base - radius` to `base + radius` where `base`
  is the current setting value, creating `steps * 2 + 1` values
- `type = "custom"`: Use explicit values provided as a numeric vector
}
\examples{
\dontrun{
model <- define_model("markov") |>
  set_settings(timeframe = 20, discount_cost = 3) |>
  add_twsa("Time vs Discount") |>
  add_twsa_setting("Time vs Discount", "timeframe",
    type = "range", min = 10, max = 30, steps = 5) |>
  add_twsa_setting("Time vs Discount", "discount_cost",
    type = "custom", values = c(0, 1.5, 3, 5))
}
}
