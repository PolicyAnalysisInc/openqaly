% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/twsa_tables.R
\name{twsa_costs_table}
\alias{twsa_costs_table}
\title{TWSA Costs Table}
\usage{
twsa_costs_table(
  results,
  outcome,
  twsa_name = NULL,
  groups = "overall",
  strategies = NULL,
  interventions = NULL,
  comparators = NULL,
  discounted = TRUE,
  decimals = NULL,
  abbreviate = FALSE,
  font_size = 11,
  backend = c("flextable", "kable")
)
}
\arguments{
\item{results}{TWSA results object from run_twsa()}

\item{outcome}{Name of the cost outcome to display (e.g., "total_cost")}

\item{twsa_name}{Name of specific TWSA analysis to show (NULL for first/only)}

\item{groups}{Group selection: "overall" (default), "all", "all_groups", or
specific group name(s)}

\item{strategies}{Character vector of strategy names to include (NULL for all)}

\item{interventions}{Intervention strategy name(s) for incremental calculation}

\item{comparators}{Comparator strategy name(s) for incremental calculation}

\item{discounted}{Logical. Use discounted values? (default: TRUE)}

\item{decimals}{Number of decimal places (default: NULL for auto)}

\item{abbreviate}{Logical. Use abbreviated formatting? (default: FALSE)}

\item{font_size}{Font size for table (default: 11)}

\item{backend}{Table rendering backend: "flextable" (default) or "kable"}
}
\value{
A rendered table object (flextable or kable)
}
\description{
Creates a grid table showing two-way sensitivity analysis cost results.
The table displays cost values across a grid with X parameter values as
columns and Y parameter values as rows.
}
\examples{
\dontrun{
results <- run_twsa(model)

# Basic cost table
twsa_costs_table(results, "total_cost")

# Incremental cost table
twsa_costs_table(results, "total_cost",
  interventions = "treatment",
  comparators = "standard_care")
}
}
