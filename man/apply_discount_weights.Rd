% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discounting.R
\name{apply_discount_weights}
\alias{apply_discount_weights}
\title{Apply Discounting Override Formulas}
\usage{
apply_discount_weights(
  values_matrix,
  values_discounted,
  trace_matrix,
  discount_factors_cost,
  discount_factors_outcomes,
  discount_cost_rate,
  discount_outcomes_rate,
  uneval_values,
  type_mapping,
  eval_vars
)
}
\arguments{
\item{values_matrix}{Matrix of undiscounted values}

\item{values_discounted}{Matrix of discounted values (modified in place)}

\item{trace_matrix}{Collapsed trace matrix (rows = cycles 0..n, cols = states),
or NULL for standalone decision tree models}

\item{discount_factors_cost}{Vector of per-cycle cost discount factors}

\item{discount_factors_outcomes}{Vector of per-cycle outcome discount factors}

\item{discount_cost_rate}{Cost discount rate (percentage)}

\item{discount_outcomes_rate}{Outcome discount rate (percentage)}

\item{uneval_values}{The unevaluated values data frame (with discounting_override column)}

\item{type_mapping}{Named vector mapping value names to types ("cost"/"outcome")}

\item{eval_vars}{The evaluated variables namespace (with $df and $env)}
}
\value{
Modified values_discounted matrix
}
\description{
For each value that has a non-empty `discounting_override` formula,
evaluates the formula and replaces the per-cycle discount factor entirely.
The formulas are user-authored model definitions (same trust level as
value/variable formulas throughout the codebase), not untrusted external input.
}
\keyword{internal}
