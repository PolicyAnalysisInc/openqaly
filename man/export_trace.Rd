% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trace_output.R
\name{export_trace}
\alias{export_trace}
\title{Export Trace Data to File}
\usage{
export_trace(
  results,
  file,
  format = NULL,
  collapsed = TRUE,
  separate_sheets = TRUE,
  ...
)
}
\arguments{
\item{results}{A openqaly model results object}

\item{file}{Output file path (extension determines format if format is NULL)}

\item{format}{Output format: "csv", "excel", "html", "json", or "rds".
If NULL, inferred from file extension}

\item{collapsed}{Logical. Use aggregated traces (TRUE, default) or segment traces (FALSE)}

\item{separate_sheets}{Logical. For Excel format, create separate sheets
for each strategy (TRUE, default) or combine into one sheet (FALSE)}

\item{...}{Additional arguments passed to format-specific functions}
}
\value{
Invisibly returns the file path
}
\description{
Exports trace data to various file formats including CSV, Excel, HTML, JSON, and RDS.
}
\examples{
\dontrun{
model <- read_model(system.file("models/example_psm", package = "openqaly"))
results <- run_model(model)

# Export to CSV
export_trace(results, "trace.csv")

# Export to Excel with separate sheets per strategy
export_trace(results, "trace.xlsx", separate_sheets = TRUE)

# Export to JSON
export_trace(results, "trace.json")
}

}
