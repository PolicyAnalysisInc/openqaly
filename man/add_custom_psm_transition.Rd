% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_builder.R
\name{add_custom_psm_transition}
\alias{add_custom_psm_transition}
\title{Add Custom PSM Transition}
\usage{
add_custom_psm_transition(model, state, formula)
}
\arguments{
\item{model}{An oq_model_builder object (must be type "custom_psm")}

\item{state}{Character string for the state name (must exist in model states)}

\item{formula}{An unquoted R expression for the state probability.
Use `C` for complement (1 - sum of other states). Only one state can use `C`.}
}
\value{
The modified model object
}
\description{
Add a state probability formula for a Custom PSM model. Each state must have
exactly one probability formula that defines the probability of being in that
state at each cycle.
}
\examples{
model <- define_model("custom_psm") |>
  add_state("alive") |>
  add_state("progressed") |>
  add_state("dead") |>
  add_custom_psm_transition("alive", surv_prob(pfs_dist, month)) |>
  add_custom_psm_transition("progressed", surv_prob(os_dist, month) - surv_prob(pfs_dist, month)) |>
  add_custom_psm_transition("dead", C)
}
