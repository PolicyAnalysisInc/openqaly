% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transition_plots.R
\name{transition_plot_heatmap}
\alias{transition_plot_heatmap}
\title{Transition Matrix Heatmap Plot}
\usage{
transition_plot_heatmap(
  res,
  cycle = 1,
  groups = "overall",
  strategies = NULL,
  collapsed = TRUE,
  decimals = 3,
  use_display_names = TRUE,
  state_times = NULL,
  exclude_zero_residency = NULL
)
}
\arguments{
\item{res}{A openqaly model results object (output from run_model)}

\item{cycle}{Integer. Which cycle's transition matrix to display (default: 1).}

\item{groups}{Group selection: "overall" (default), specific group name,
vector of groups, "all", "all_groups", or NULL}

\item{strategies}{Character vector of strategy names to include (NULL for all)}

\item{collapsed}{Logical. If TRUE (default), use collapsed state names.
If FALSE, use expanded state names (tunnel states).}

\item{decimals}{Integer. Number of decimal places for probability labels (default: 3).}

\item{use_display_names}{Logical. If TRUE (default), use display names for entities.}

\item{state_times}{Numeric vector of tunnel state indices to include when
\code{collapsed=FALSE}. Use \code{Inf} for the last tunnel state of each
base state. Non-tunnel states are always included. Ignored when
\code{collapsed=TRUE}.}

\item{exclude_zero_residency}{Logical. Exclude expanded states with zero
residency at the plotted cycle. Defaults to TRUE when \code{collapsed=FALSE},
FALSE when \code{collapsed=TRUE}. Requires \code{cycles} to be specified.}
}
\value{
A ggplot2 object
}
\description{
Visualizes transition probabilities as a heatmap for a specific cycle.
}
\examples{
\dontrun{
model <- read_model(system.file("models/example_psm", package = "openqaly"))
results <- run_model(model)

# Basic heatmap for cycle 1
transition_plot_heatmap(results)

# Heatmap for cycle 5 with more decimal places
transition_plot_heatmap(results, cycle = 5, decimals = 4)
}

}
