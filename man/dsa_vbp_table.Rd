% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dsa_vbp_tables.R
\name{dsa_vbp_table}
\alias{dsa_vbp_table}
\title{DSA+VBP Table}
\usage{
dsa_vbp_table(
  results,
  wtp = NULL,
  comparators = "all",
  groups = "overall",
  decimals = 0,
  font_size = 11,
  ...
)
}
\arguments{
\item{results}{DSA+VBP results object from run_dsa() with VBP enabled}

\item{wtp}{Willingness-to-pay threshold for calculating VBP. If NULL (default),
uses the WTP from the outcome summary specified in the VBP analysis.}

\item{comparators}{Comparator selection: "all" (default, shows each comparator + aggregate),
"overall" (aggregate only), "all_comparators" (individuals only), or specific comparator name(s)}

\item{groups}{Group selection: "overall" (default), "all" (overall + all groups),
"all_groups" (all groups without overall), or specific group name(s)}

\item{decimals}{Number of decimal places for VBP values (default: 0)}

\item{font_size}{Font size for rendering (default: 11)}

\item{...}{Additional arguments (reserved for future use)}
}
\value{
A rendered table object (gt or equivalent)
}
\description{
Creates a table showing value-based prices at a specified WTP threshold
for each DSA parameter variation (low, base, high).
}
\examples{
\dontrun{
# Create table using default WTP from model
dsa_vbp_table(results)

# Create table at specific WTP
dsa_vbp_table(results, wtp = 50000)

# Create table for specific comparator
dsa_vbp_table(results, wtp = 50000, comparators = "chemo")

# Create table for all groups with 2 decimal places
dsa_vbp_table(results, groups = "all", decimals = 2)
}

}
