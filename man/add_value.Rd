% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_builder.R
\name{add_value}
\alias{add_value}
\title{Add Values to Model}
\usage{
add_value(
  model,
  name,
  formula,
  state = NA,
  destination = NA,
  display_name = NULL,
  description = NULL,
  type = "outcome",
  discounting_override = NA
)
}
\arguments{
\item{model}{A oq_model_builder object}

\item{name}{Character string for the value name}

\item{formula}{An unquoted R expression for the value calculation}

\item{state}{Optional state association}

\item{destination}{Optional destination state}

\item{display_name}{Optional display name}

\item{description}{Optional description}

\item{type}{Value type (default: "outcome")}

\item{discounting_override}{Optional formula string that overrides per-cycle
discounting. When provided, the formula result replaces the standard discount
factor entirely: \code{values_discounted = values * result}. The formula can
return a scalar or a vector of length n_cycles. Available variables in the
formula: all model variables, \code{discount_rate}, \code{discount_factors},
and \code{trace(state_name)}.}
}
\value{
The modified model object
}
\description{
Add one or more values to the model. Uses NSE to capture formula expressions.
}
\examples{
model <- define_model("markov") |>
  add_value("cost", base_cost + extra_cost, state = "sick")
}
